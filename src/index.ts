import {showSchemasList, createFormFromSchemaSelection} from "./main"
import "./assets/styles.css"

(window as any).showSchemasList = showSchemasList;
(window as any).createFormFromSchemaSelection = createFormFromSchemaSelection;